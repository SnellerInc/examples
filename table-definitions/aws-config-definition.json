{
    "input": [
      {
        "pattern": "config-bucket-XXXXXXXX/AWSLogs/ACCOUNT-ID/Config/{region}/*/*/*/*/*.log.gz",
        "format": "json.gz"
      }
    ],
    "partitions": [ { "field": "region" } ]
}